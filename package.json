{{

  "name": "idoso-ms",  "name": "idosoms",

  "private": true,  "version": "1.0.0",

  "version": "1.0.0",  "description": "Plataforma para avaliações de índices de vulnerabilidade de idosos em MS.",

  "type": "module",  "main": "public/src/main.js",

  "description": "Sistema de avaliação de índices de vulnerabilidade de idosos - Mato Grosso do Sul",  "scripts": {

  "scripts": {    "test": "echo \"Error: no test specified\" && exit 1",

    "dev": "vite",    "build:css": "tailwindcss -i ./public/css/input.css -o ./public/css/style.css --watch"

    "build": "vite build",  },

    "preview": "vite preview",  "keywords": [],

    "deploy": "firebase deploy --only hosting"  "author": "",

  },  "license": "ISC",

  "dependencies": {  "devDependencies": {

    "firebase": "^10.7.1",    "tailwindcss": "^3.4.1"

    "chart.js": "^4.4.0",  }

    "jspdf": "^2.5.1",}
    "papaparse": "^5.4.1",
    "workbox-window": "^7.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.8",
    "vite-plugin-pwa": "^0.17.4",
    "@tailwindcss/forms": "^0.5.7",
    "tailwindcss": "^3.3.6",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32"
  }
}